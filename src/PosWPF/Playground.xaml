<Window x:Class="PosWPF.Playground"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Width="300" Height="300">
    <DockPanel LastChildFill="True">
        
        <DockPanel DockPanel.Dock="Top" LastChildFill="True">
            <Button DockPanel.Dock="Left" Background="{StaticResource PlusIcon}" Width="30" Height="30" Style="{StaticResource BorderlessButton}"/>
            <Button DockPanel.Dock="Right" Background="{StaticResource SearchIcon}" Width="30" Height="30" Style="{StaticResource BorderlessButton}"/>
            <TextBox />
        </DockPanel>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Vertical">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Username" Width="80" TextAlignment="Right" Margin="0,0,10,0" />
                <TextBox Width="200" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Password" Width="80" TextAlignment="Right" Margin="0,0,10,0" />
                <TextBox Width="200" />
            </StackPanel>
        </StackPanel>
        
        <ListView />
    </DockPanel>
</Window>